# Python Agent：计划审阅与日级学习路线

编制日期：2026-09-17。审阅对象：`/Users/wy/stock/agent.md`。原文件保持原样，本文件是独立修订稿。

## 1. 针对你的情况，先调整终点

你已经确认：熟悉 Python、测试、数据库与部署；主要为了学习 Agent 原理，产品用途尚未确定；每天至少投入 1 小时；在 Mac M2 笔记本本地运行，通过 API 使用大模型。

建议采用：**8 周原理学习与实验，另加用途确定后的 4 周产品化参考计划。** 前 8 周约 56 小时，其中 48 小时推进新内容、8 小时复盘缓冲。后 4 周约 28 小时，必须在选定用途后重新估算，不能视为任何 Agent 都能按时上线的承诺。

**现阶段不适合把“达到生产级”设为唯一结业条件。** 用途未定时，生产级没有可检验的含义，容易让学习变成无止境的基础设施建设。更合适的第一阶段目标是：

> 亲手实现一个可解释、可测试、有执行边界的单 Agent 运行程序；用实验理解工具调用、上下文、状态、记忆、规划和自检的作用；完成从输入资料到输出成果的闭环，并能判断某项能力是否值得加入。

生产级保留为第二阶段目标：选择一个重复出现的真实任务后，让它在声明的范围内可靠、可恢复、可维护。生产意识从第一天贯穿，生产发布按场景和证据决定。

原文中的“不使用框架”“严格按 V0–V10 顺序”等内容属于待评估的设计选择，不视为你已经确认的要求。

### 已确认条件与计划默认值

| 项目 | 采用的安排 |
|---|---|
| 开发基础 | 跳过 Python、Git、测试和数据库入门，直接用于理解 Agent 特有问题 |
| 时间 | 每天以 60 分钟为必做上限；额外时间用于补实验或阅读，不提前塞入必做范围 |
| 周节奏 | 每周 6 天推进、1 天复盘/缓冲；缺一天则顺延，不要求次日双倍补课 |
| 本地环境 | Mac M2 运行 Python、状态存储与工具；模型通过 API；不安排模型训练或本地大模型部署 |
| 模型 | 一个支持工具调用的服务及官方 SDK；不先建设多模型适配框架 |
| 界面 | 命令行；教学阶段不开发网站、移动端或多人账号 |
| 教学载体 | 暂选“指定资料检索 → 必要时查询公开网页 → 输出带来源的 Markdown 笔记”；这是练习题，不是确定的产品方向 |
| 输入 | 自制、无敏感信息的 Markdown/TXT；扫描 PDF、复杂 Excel/OCR 不作为必修依赖 |
| 费用 | 服务商与月预算尚未确定；D01 设置月预算 M、单次预算 B 和评测预算 E 后再批量调用 |
| 日期 | 未确认开始日期，使用 D01–D56、P01–P28 学习日编号；没有自动创建日历或提醒 |

## 2. 原计划遗漏与修订

| 原计划的问题 | 为什么需要调整 | 本版补充 |
|---|---|---|
| 能力清单没有对应实验和交付物 | 看懂术语不等于理解运行机制 | 每天一个小改动、一个验证和可保存结果 |
| V0→V10 是严格必经路线 | 容易把模块数量等同于能力和质量 | 核心机制必修；高级能力做对照实验，是否上线另行决定 |
| V10 才整合 | 集成问题集中到最后 | 每周保留可运行版本，第 2 周已有本地资料闭环 |
| 未比较固定流程与 Agent 循环 | 无法判断自主选择动作是否值得 | 同一任务比较固定步骤、基础循环、显式计划 |
| Context 没有单列 | 全量历史和工具输出挤占上下文 | 第 3 周专门做上下文、分块、检索和缺证据实验 |
| State、History、Memory 混在一起 | 恢复、个性化和知识检索职责不清 | 分开讨论运行状态、会话历史、偏好和资料索引 |
| 评测直到 V9 才出现 | 不知道新增机制是否有效 | 第 1 周建立运行记录和小样例，持续加入失败案例 |
| Loop 主要依赖最大次数停止 | 仍会超时、超预算、无效重复 | 加入时长、费用、无进展、取消与有限重试 |
| 工具只有名称和函数接口 | 错误参数可能造成真实副作用 | 输入/输出/错误契约、权限、超时、输出大小和副作用属性 |
| 文件操作缺少变更验证 | 可能覆盖、留下半成品或误报成功 | 输入只读、输出隔离、不覆盖；检查实际文件，学习原子提交与恢复 |
| V5 过早引入任意 Python | 学习项目承担过大的隔离工程 | 固定工具先行，通用代码执行只做后期可选隔离实验 |
| 网页内容和引用缺少信任边界 | 可能遵循材料内指令，或把摘要当已验证事实 | 分离资料与指令，保存证据和时间，测试注入与抓取失败 |
| Memory 只有记住与检索 | 错误信息会持续影响任务 | 显式偏好、来源、时间、更正、删除、冲突与污染实验 |
| Reflection 缺独立判据 | 自评通过不能证实正确 | 程序检查实际产物优先，模型评审辅助且限制修正次数 |
| 缺持久化、恢复与幂等 | 重启和超时后可能重复写入 | 保存动作记录；区分失败与结果未知，先核对再重做 |
| 不使用框架是永久约束 | 教学选择可能变成维护负担 | 前期手写机制，结业对照成熟组件后再决定 |
| 缺工时、缓冲与阶段出口 | 每日任务无限扩张 | 每周缓冲，学习/选修/产品化分开管理 |

需要纠正五个表述：

1. 工具在本项目中可用 Python 函数封装，背后也可能是远程 API 或服务。
2. 程序接口中的“LLM 思考”应落实为“模型返回下一步动作或最终输出”，不依赖获取隐藏推理过程。
3. Memory、显式 Planner、Reflection 不是所有 Agent 的必选组成。
4. 能执行 Python 不等于拥有通用电脑操作能力。
5. `subprocess`、虚拟环境、timeout 和提示词不能单独构成任意代码执行的安全沙箱。

先使用简单方案，只有效果证明需要时才增加自主性，是本计划采用的设计原则；工作流与 Agent 的区别可参见 [Anthropic：Building effective agents](https://www.anthropic.com/engineering/building-effective-agents)。本计划的工时、排期和阈值是针对当前情况的建议，不是该资料给出的承诺或行业标准。

## 3. 学习与产品分别验收

| 学习阶段需要证明 | 产品阶段另需证明 |
|---|---|
| 能解释模型、运行程序和工具分别负责什么 | 支持的真实任务能重复完成 |
| 能追踪参数、结果、状态与停止原因 | 失败、取消、超时和重启后行为可控 |
| 能比较有无记忆、规划和自检 | 启用的能力有实际价值，维护成本可接受 |
| 能区分模型错误、工具错误、材料缺失与评分错误 | 权限、数据外发、恢复、安装与升级都经过验证 |
| 能独立修改机制并复现实验 | 有真实试用记录、已知限制与明确发布结论 |

第 8 周交付“教学 Agent v0.1 + 实验报告”。尚不适合长期处理真实资料也可以完成学习；功能很多却无法解释机制的演示，则不算结业。

最小工程组成：一个模型服务、一个命令行入口、一个循环、少量工具、一种状态存储、简单的测试与评测脚本。模块从 model、runtime、tools、storage、evals 起步即可；不要预建复杂目录，避免原文两个位置同时出现 Planner 却职责不清。

不要求向量数据库、多 Agent、分布式队列、插件市场、浏览器自动化、云部署、完整前端或监控平台。MCP 与框架安排为结业对照课。

每天建议：10 分钟阅读 → 35 分钟实现/实验 → 10 分钟验证 → 5 分钟记录。超时则记录已完成、未完成与下一步，进入缓冲日。环境或接口阻塞时可使用固定样本/模拟返回，但必须标记并安排真实补验。

## 4. 第一阶段：56 个学习日

### 第 1 周：API、工具调用、最小循环（V0–V2）

| 日 | 当天任务与产物 | 验收方式 |
|---|---|---|
| D01 | 选定 API 并运行最小调用；保存配置示例、SDK/模型版本与预算参数，密钥与代码分离 | 能输出响应和可获得的用量；代码/日志无密钥；预算未定时先用模拟数据 |
| D02 | 比较消息角色、普通回答与结构化输出；相同输入运行 3 次，记录差异、终止状态与费用 | 能解释 JSON 输出与请求执行工具不同；截断、拒绝不算成功 |
| D03 | 实现 calculator：明确操作符和两个数值参数，定义工具输入输出 | 正常计算、非法操作符、参数类型错误都有确定结果；不用任意字符串 eval |
| D04 | 实现“模型请求工具 → 程序执行 → 回传结果 → 模型回答”，保留调用关联标识 | 从记录对应请求和结果；能证明模型没有自行执行 Python |
| D05 | 扩展为循环，设置步数、总时长、调用额度与无进展停止条件 | 制造重复调用，程序能结束并说明真实停止原因 |
| D06 | 添加 run_id、调用次数、工具状态、耗时、用量记录，建立 8 个小用例 | 覆盖直接回答、正常计算、非法参数、工具失败与循环停止；失败不记成完成 |
| D07 | 复盘/缓冲：补缺口，手画调用链，演示成功与失败任务 | 不增加新能力；能不看代码解释链路，保存本周版本 |

### 第 2 周：工具契约与文件闭环（V3–V4）

| 日 | 当天任务与产物 | 验收方式 |
|---|---|---|
| D08 | 建立小型注册表，统一工具名称、参数、结果、错误和读写属性 | 未注册工具及非法参数由程序拒绝；不做动态安装或插件市场 |
| D09 | 添加授权目录列举与读取，限制体积，检查规范化路径与符号链接 | 正常文件能读；目录穿越、越界链接、超大文件被拒绝 |
| D10 | 添加本地文本搜索，返回文件、位置、片段与数量上限 | 能找到预期片段；无结果时返回空，不编造文件 |
| D11 | 添加专用目录内的新文件创建，临时文件验证后提交，禁止覆盖 | 成果真实存在；写入中断或目标已存在时不破坏输入资料 |
| D12 | 接通“读取指定资料 → 提取要点 → 写 Markdown”，使用 3 份自制文档 | 打开实际文件核对要点和来源，不能只说已保存 |
| D13 | 加入缺输入时澄清与用户取消；区分已授权自动操作和越界操作 | 正常读取不反复确认；越界不执行；取消后不再发起新动作 |
| D14 | 复盘/缓冲：整理失败并重跑已有案例；余量允许时补到 12 个开发用例，预留 6 个结业用例 | 闭环能独立演示；预留用例不用于逐题调提示词，未补齐则顺延 |

### 第 3 周：上下文与检索

| 日 | 当天任务与产物 | 验收方式 |
|---|---|---|
| D15 | 区分会话历史、工作上下文、运行状态、长期记忆；逐步加入无关内容做实验 | 记录输入长度、费用、是否丢约束，画出四者职责表 |
| D16 | 给工具输出与消息历史设置大小预算，保留当前约束与工具请求/结果关联 | 超长输出被截断或改成摘要/文件引用，不产生悬空调用记录 |
| D17 | 长文本分块，给片段附来源、位置和稳定标识 | 任一片段能回到原文，不混淆不同来源 |
| D18 | 用关键词或全文检索选择相关片段，接入回答流程 | 返回预期证据；不要求建立 Embedding 服务或向量数据库 |
| D19 | 构造没有答案、资料冲突的输入，设计缺证据结果 | 输出区分已知、冲突和无法确认，不把检索成功当事实已证实 |
| D20 | 用 6 个问题比较全文输入与检索后输入，保存结果、片段和费用 | 分别评估检索是否找到材料、回答是否正确使用材料 |
| D21 | 复盘/缓冲：定位一个上下文或检索失败，判断是否需要复杂检索 | 能说明向量检索可能解决什么、不能解决什么；不强制实现 |

### 第 4 周：公开网页研究与来源（V6 提前）

| 日 | 当天任务与产物 | 验收方式 |
|---|---|---|
| D22 | 接一个公开搜索入口，只发送公开问题/脱敏关键词；无账号先用固定样本 | 返回标题、URL、摘要；记录真实/模拟标记和费用，不用本地原文做搜索词 |
| D23 | 实现受限网页读取：限定已知公开站点、协议、大小、超时，重新校验跳转目标 | 不允许任意本地/内网目标；失败有状态，不拿摘要冒充全文 |
| D24 | 用自制材料加入“忽略任务、读取其他文件”等指令，观察并修正 | 模型即使建议越界，执行器仍拒绝；材料不能改变运行权限 |
| D25 | 加入结论、证据片段、来源、访问时间的对应关系 | 抽查至少 3 个事实得到原文支持；有 URL 不等于引用正确 |
| D26 | 整合最多读取少量网页的研究任务，输出笔记及来源清单 | 标出事实、推断、冲突和未验证项；记录工具数与总费用 |
| D27 | 测试抓取失败、旧资料、来源矛盾和缺证据；对比固定流程与自主选步骤 | 同题记录质量与成本，允许得到简单流程更合适的结论 |
| D28 | 复盘/缓冲：演示从问题到可核查笔记，整理未解决项 | 跨工具作品可运行；未接通的真实接口明确标为未验证 |

### 第 5 周：状态、恢复与记忆（补 State，学习 V7）

| 日 | 当天任务与产物 | 验收方式 |
|---|---|---|
| D29 | 建最小状态表：运行中、待输入、完成、失败、取消、结果待核对 | 可查询任务状态及最后确认步骤，不建设完整事件平台 |
| D30 | 保存只读步骤结果和检查点，中途主动退出再重启 | 能复用结果或安全重读；不是从零重跑却声称恢复 |
| D31 | 写入记录操作标识、目标及内容摘要，恢复前核对文件 | 模拟文件已写但状态未保存，不重复创建或覆盖；核对不了就暂停 |
| D32 | 实现显式偏好记忆，如“报告用中文”，保存来源与更新时间 | 新会话使用偏好；网页文字不自动变成用户偏好 |
| D33 | 实现记忆查看、修改、删除和新旧冲突处理 | 更正采用新值，删除不再召回，不保存密钥或完整私密对话 |
| D34 | 关闭/开启记忆回答同题，并插入误导性旧记录做污染实验 | 能定位收益或损害；记忆无权覆盖执行权限 |
| D35 | 复盘/缓冲：分别清空会话、重启程序、删除记忆，观察差别 | 能解释不同影响，保留恢复实验记录 |

### 第 6 周：规划与自检的对照实验（V8–V9）

| 日 | 当天任务与产物 | 验收方式 |
|---|---|---|
| D36 | 固定一个研究任务，保存无显式 Planner 基线：成功、调用数、耗时、费用 | 先定义成功，不以“看起来更聪明”评价 |
| D37 | 增加最多 3 步的结构化计划，含预期产物与步骤状态 | 计划可解析、可执行，不建复杂任务树 |
| D38 | 来源不可用时允许最多一次重新规划 | 能改走替代路径或停止，不无限扩任务范围 |
| D39 | 添加确定性检查：文件存在、必填部分、可追踪引用、必要计算 | 检出故意制作的错误结果，检查产物而非成功措辞 |
| D40 | 尝试有规则、有证据的模型评审，最多修正一次 | 人工对照已知错误，记录漏检误报；自评分不决定权限或真伪 |
| D41 | 同一批 6 个任务比较基础版、加规划版、加检查版 | 同时记录质量、费用、延迟；输入一致，不同时改多个变量 |
| D42 | 复盘/缓冲：写一页结论，决定各机制默认开或关 | 用数据说明取舍，既不要求所有模块改善，也不预设某个模块无用 |

### 第 7 周：Python 工具与执行边界（V5 后移）

必修是理解执行机制并实现固定工具；任意模型代码执行是选修，不把自研生产沙箱作为一周目标。

| 日 | 当天任务与产物 | 验收方式 |
|---|---|---|
| D43 | 用自己写的无害固定脚本学习进程、stdout/stderr、退出码、超时 | 正常、异常、超时可区分；不执行模型任意代码 |
| D44 | 把计算或合成 CSV 汇总做成参数明确的固定工具，约束输入输出 | 与已知答案一致，不接受额外命令、任意路径或任意脚本 |
| D45 | 比较固定工具、任意代码、隔离执行，画文件/网络/密钥边界 | 能解释 venv 和 timeout 不是沙箱，说明当前真实边界 |
| D46 | 选修 A：在兼容 Apple silicon 的一次性隔离环境运行固定脚本；替代 B：完善固定工具失败输出 | A 仅挂载专用练习目录，不带密钥、不开放网络和宿主控制接口；未准备好就用 B |
| D47 | A：验证时间、内存、进程、输出限制与清理；B：验证超大输入、异常数据、取消 | A 未通过不接模型代码；B 明确仍是固定工具 |
| D48 | A 全部验证通过后试一个无敏感数据的简单代码生成任务；否则 B 完成相同计算任务 | 保存结果与执行记录，不把一次成功当成通用执行已生产可用 |
| D49 | 复盘/缓冲：比较固定工具与通用执行必要性，决定默认启用项 | 选修未完成明确记录；固定工具仍能满足结业要求 |

若选择 Docker Desktop，先核对当前 [Mac 安装要求](https://docs.docker.com/desktop/setup/install/mac-install/) 与镜像 ARM64 兼容性。隔离取决于权限、挂载、网络及宿主配置，不能仅凭“在容器中”判断安全。[Docker Engine security](https://docs.docker.com/engine/security/)

### 第 8 周：整合、评测与结业（V10 重新定义）

| 日 | 当天任务与产物 | 验收方式 |
|---|---|---|
| D50 | 整理单一入口、配置和开关，删除无用分支 | 记忆、计划、评审可明确开关，不依赖旧进程隐含状态 |
| D51 | 集中运行超时、非法参数、重复动作、预算耗尽、缺资料和取消案例 | 各有真实状态、停止原因和记录，没有误报完成 |
| D52 | 运行预留 6 个结业任务，每个 3 次独立试验，重置环境 | 报告成功次数/总次数、失败分类和费用；预算不足先跑完整单轮，余下整体顺延，不挑选成功运行 |
| D53 | 汇总上下文、记忆、规划、自检对照；只补最大疑点 | 得到“问题—变更—证据—代价—结论”表，不大面积重跑 |
| D54 | 在干净目录/环境按说明安装，整理样例、配置、日志位置 | 文档能复现成功与失败案例，依赖和模型配置可追踪 |
| D55 | 对照 Runtime/Harness、框架/SDK、MCP 各自职责，阅读一个官方示例 | 能映射自己的循环、工具、状态，不要求重写或接新服务 |
| D56 | 结业复盘/缓冲：演示、讲解、整理教学版本与报告，决定是否产品化 | 对照清单；未通过就补实验，不因到期自动结业 |

### 第一阶段结业清单

- [ ] 能还原模型请求、工具请求、宿主执行、结果回传和终止。
- [ ] 能独立修改一种停止策略，并验证有效。
- [ ] 能解释并演示上下文、状态、历史、检索和用户记忆的区别。
- [ ] 至少有一个完整本地资料任务；网页模拟部分明确标记并安排补验。
- [ ] 能发现“模型声称成功，但实际没有产物”的问题。
- [ ] 规划、记忆、自检均有实验，结论可以是保留、关闭或证据不足。
- [ ] 不可信资料不能通过执行器扩大权限；知道测试通过不等于没有漏洞。
- [ ] 能处理重复、超时、取消、预算不足，区分写入失败与结果未知。
- [ ] 有代码、配置示例、无敏感样例、简短说明与实验报告。
- [ ] 能说明缺少哪些生产能力，以及是否值得继续产品化。

## 5. 第二阶段：28 个产品化参考日（可选）

启动条件：能说出“我每周反复做的哪个任务，要由 Agent 完成哪一部分”，并确定输入、输出、频率、错误代价与权限。用途未明确时可以停在学习阶段，或另选深入研究方向。

以下仅适用于**单用户、本机按需运行、读取授权资料、在专用目录生成成果的低风险助手**。多人服务、交易执行、外部消息发送、广泛文件修改、长期无人值守都要重新估算，不在 28 小时承诺内。

### 第 9 周：真实需求与验收口径

| 日 | 当天任务与产物 | 验收方式 |
|---|---|---|
| P01 | 选一个重复真实任务，记录手工耗时、输入、成果 | 一句话产品目标；无真实任务就暂停产品化，保留教学成果 |
| P02 | 一页范围与权限清单，说明支持/不支持及发往 API 的数据 | 每个工具对应需求，边界不由模型自行决定 |
| P03 | 定义成功、人工协助、失败、拒绝、费用、耗时口径 | 用 3 个任务试评分，无异常退出不等于成功 |
| P04 | 按真实任务精简教学代码与默认开关 | 不需要的规划、记忆、通用代码可关闭，主流程仍可用 |
| P05 | 准备 15 个正常验收任务和至少 10 个故障/权限案例及预期结果 | 两套分开，覆盖支持的边界，不对验收题逐题调提示词 |
| P06 | 整理启动、范围、进度、取消、输出、失败说明 | 按说明可用；已授权常规动作不反复确认 |
| P07 | 复盘/缓冲：核对范围，重新估算剩余工作 | 超范围就缩减或顺延，用途变化不沿用旧结论 |

### 第 10 周：发布候选的必需检查

| 日 | 当天任务与产物 | 验收方式 |
|---|---|---|
| P08 | 复查文件、符号链接、外部内容、网页地址与重定向的执行边界 | 正反案例均符合预期；不能判断是否合法则不执行 |
| P09 | 演练中断、写入结果未知、重启和重复提交 | 不盲目重放副作用；成果可核对，核对不了明确暂停 |
| P10 | 检查模型/搜索外发、密钥、日志原文、偏好与删除 | 有数据流和保留规则，敏感原文默认不进搜索/调试日志 |
| P11 | 完成费用预留、总时间、退避重试和取消检查 | 仅重试暂时故障；没有足够预算不发起新调用 |
| P12 | 演练干净安装、备份恢复、回到上一版本，记录操作 | 真正恢复并验证数据兼容；只有备份文件不算通过 |
| P13 | 正常套件各运行 2 次，故障套件覆盖关键分支，记录版本结果费用 | 达到下文门槛且无关键未修复问题；时间不够顺延 |
| P14 | 复盘/缓冲：补缺口，证据齐全才冻结候选 | 未验收不进入正式试运行；重大修改补回归并延长观察 |

### 第 11–12 周：真实试运行与发布判断

P15–P28 是至少 14 个自然日观察窗口，每天投入仍约 1 小时。累计真实任务不足 20 次则延长，不能重复同一演示凑数；只试用已验收范围。

| 日 | 当天任务与产物 | 验收方式 |
|---|---|---|
| P15 | 首次真实任务，记录范围、产物和核对时间 | 核查最终结果，有问题分类记录 |
| P16 | 使用不同材料完成真实任务，检查事实和来源 | 重要事实有支持，缺材料明确不足 |
| P17 | 汇总真实费用、调用数、耗时，与手工基线比较 | 失败和修正成本计入，不只统计成功 |
| P18 | 在非关键任务中演练中断或笔记本休眠后重启 | 状态真实，不默默重复写入 |
| P19 | 归因主要失败，修复最高价值问题 | 跑相关回归，重大改动重新计算观察窗口 |
| P20 | 记录人工介入：补输入、改结论、代做核心步骤 | 正常澄清与人工代做分开计数 |
| P21 | 复盘/缓冲：检查真实记录、价值与进度 | 没有价值则回到需求，不为上线凑案例 |
| P22 | 用第二类受支持材料完成任务，检查覆盖 | 不在未验收任务上声称稳定，新需求另记 |
| P23 | 核查来源变化、失效或资料更新后的表现 | 有时间与限制，旧缓存/旧记忆不冒充当前事实 |
| P24 | 抽查日志、数据保留与清理、累计费用 | 存储、访问、删除可解释，清理不损坏活动任务 |
| P25 | 汇总故障恢复情况，关闭不稳定选项 | 核心任务仍合格，不为功能表保留高故障模块 |
| P26 | 汇总至少 20 次真实任务：成功失败、介入、费用、耗时 | 使用实际分子分母，样本或时长不足就延长 |
| P27 | 整理安装、恢复、限制、版本清单与后续需求 | 文档与版本一致，每项标准有证据 |
| P28 | 发布评审/缓冲：发布个人 v1.0、继续试用或停止产品化 | 按门槛决定，不把到期或功能齐全当通过 |

## 6. 个人生产版本的建议验收标准

下列数值是讨论起点，不是行业标准。P03 按风险和手工基线调整，在正式验收前冻结。若改变范围或门槛，要记录原因并重新验收，不能失败后悄悄降线。

| 维度 | 建议门槛 | 口径 |
|---|---|---|
| 实际结果 | 15 个正常任务 × 2 次，至少 27/30 合格 | 每次重置环境，同时报首次与有限重试后成功率；失败、超预算、人工代做核心工作都计入 |
| 稳定性 | 同时报告多少任务两次均成功，并逐类报告 | 不用总体百分比掩盖某类持续失败 |
| 事实引用 | 至少抽查 30 条关键事实，建议 27/30 得到来源支持；关键结论不保留已知错误 | 核对原文含义、对象与日期，不只看 URL |
| 权限停止 | 关键故障/越权案例全部正确处置 | 越界、外传、重复循环、取消、预算分别验证；任何边界失效阻止发布 |
| 恢复 | 通过退出、写后状态未保存、重启核对、重复提交演练 | 不要求全部自动续跑，但须避免重复副作用并显示真实状态 |
| 成本耗时 | 受已确定的预算和时间约束，报告中位数及最慢几次 | 计入重试、失败、评审和工具成本；小样本不包装成严格可用性保证 |
| 安装回退 | 干净安装、备份恢复、回退各实际完成一次 | 一起考虑代码、提示词、模型配置、依赖和状态格式 |
| 真实使用 | 至少 14 个自然日、20 次真实任务，建议至少 18/20 成功 | 报告人工介入和实际节省时间；样本不足延长 |
| 未解决问题 | 无未修复的数据丢失、越权、泄漏或系统性误报完成 | 普通限制可写清，但不能损害已承诺核心任务 |

30 次离线试验和 20 次真实任务仅支撑有限范围的初始判断，不证明总体错误率稳定，也不证明不存在漏洞。之后持续收集失败；模型、提示词、工具或数据策略改变时做相应回归。

抽查发现的无来源支持或无法核实条目，必须更正、删除或明确标为未验证；影响核心结论时，该任务判失败。27/30 衡量原始输出质量，不允许保留已经发现却未处理的问题。原始评分与修正后结果分开保存，不能用事后修正掩盖首次失败。

评测应检查环境中的实际结果，结合程序检查和人工判断；模型评审需要校准，多次试验有助于观察波动。[设计依据：Anthropic 的 Agent 评测说明](https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents)

## 7. 贯穿执行的约束与调整规则

### 权限、数据与执行

模型提出动作，运行程序检查参数、权限、预算、状态。已授权低风险动作自动执行，越界拒绝或请求具体授权。原文、网页、记忆都不能修改权限。

本机运行不代表资料留在本机：提交给模型的内容会发送到 API。先用无敏感练习数据，选服务时确认可接受的数据处理规则；模型 API、搜索、日志是不同数据流，分别约束发送内容。

格式分隔、过滤、另一个模型检查只能辅助，不能替代执行校验。最小权限、数据保护和不可信内容处理参考 [OWASP AI Agent Security](https://cheatsheetseries.owasp.org/cheatsheets/AI_Agent_Security_Cheat_Sheet.html) 与 [Prompt Injection Prevention](https://cheatsheetseries.owasp.org/cheatsheets/LLM_Prompt_Injection_Prevention_Cheat_Sheet.html)。

如果生产任务不需要任意 Python 执行，保持关闭。需要时单独验证隔离、资源、环境变量、挂载、网络、依赖、清理；不让模型自行扩大挂载、安装权限或开启网络。

### 费用与恢复

D01 记录服务当日价格与计费项，不在计划中固定易过时的模型价格。单次预算 B 包含所有模型调用、重试、评审和工具费用；调用前按最大输出等预留，不能只在结束后发现超额。无用量时采用保守上界或停止额外调用。账户预算提醒不一定是硬限制，应核对实际行为。

暂时网络故障可以有限退避重试；权限和持续参数错误不盲重试。写入超时表示结果未知，先核对文件/执行记录；只在本地生成幂等标识，不意味着执行端已保证去重。

### 落后或范围变化时

先减任意代码选修，再减文件类型和外部来源，再简化记忆/规划/评审实现。保留机制对照、执行边界、运行记录和复盘。每两周重估实际进度；连续两天被外围工程阻塞，就先用固定样本验证原理，并单列真实集成补验。

多人服务另排身份、数据隔离、并发、配额、对外接口和运营保障；长期无人值守另评笔记本休眠、网络、调度与恢复；广泛文件修改或任意代码另立执行边界项目。界面需求不应替代原理学习。

## 8. 每日记录与阅读材料

```text
学习日 / 实际日期：
今天要回答的问题：
完成的小改动或实验：
输入与配置版本：
预期结果 / 实际结果：
运行记录或产物位置：
调用数 / 费用估算 / 实际投入分钟：
今天能独立解释的机制：
失败原因或证据不足：
下一步（只写一件事）：
```

阅读计入每天 10 分钟，不要求先通读长文：

| 时间 | 官方资料 | 目的 |
|---|---|---|
| 第 1 周 | 所选模型服务的工具调用、结构化输出、用量文档 | 对照实际请求、关联标识、状态、计费字段 |
| 第 2–6 周 | [Building effective agents](https://www.anthropic.com/engineering/building-effective-agents) | 比较工作流、循环、规划与评审 |
| 第 1、6、8 周 | [Demystifying evals for AI agents](https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents) | 区分任务、试验、评分、轨迹与结果 |
| 第 2、4、5 周 | [OWASP AI Agent Security](https://cheatsheetseries.owasp.org/cheatsheets/AI_Agent_Security_Cheat_Sheet.html) | 对照工具、记忆、数据与权限，不照搬企业制度 |
| 第 7 周选修 | [Docker Engine security](https://docs.docker.com/engine/security/) | 理解配置、权限和挂载影响 |
| D55 | [MCP 官方入门](https://modelcontextprotocol.io/docs/getting-started/intro) | 理解连接协议职责，它不代替规划、授权或质量验收 |

实际开工前再确定开始日期、API 服务和月预算上限。真实产品用途可以在第一阶段后半程决定，不必为了生产级标签提前制造需求。
